import 'package:stacked/stacked.dart';

abstract class NotificationService {
  Future<String> getSmsMessage();
                                   // Blue print of what our service will start by doing

}